{% block header %}

    <style>

    * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        a {
            text-decoration: none;
            color: white;
        }
        header {
            width: 100%;
            height: 5rem;
            color:white;
            padding: 0 2rem;

            display: flex;
            flex-direction: row;
            justify-content: space-between;
            align-items: center;

            background-color: #2c3e50;
            font-family: 'Roboto', sans-serif;
            
        }

        header ul {
            list-style: none;
            display: flex;
            flex-direction: row;
            gap: 2rem;
        }


    </style>

    <header>
        <h1> COOK3O</h1>

        {% if username %}
                <ul >
                    <li><a href="{{ path('app_home')}}">Accueil</a></li>
                    <li><a href="{{ path('app_article')}}">Ajouter un article</a></li>
                    <li><a href="{{ path('app_logout')}}"><i class="fa-solid fa-arrow-right-from-bracket"></i></a></li>
                </ul>

        {% else %}

                <ul><li><a href="{{ path('app_login')}}"><i class="fa-solid fa-right-to-bracket"></i></a></li></ul>
        {% endif %} 

    </header>

{% endblock %}